using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class OpenLearning : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //GetComponent<RawImage>().color = new Color32(224, 209, 9, 120);
        //Name = "Open Learning";
        //Regions.Add(this);
    }
}
