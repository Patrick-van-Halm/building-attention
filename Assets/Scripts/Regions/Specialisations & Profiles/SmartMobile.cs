using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class SmartMobile : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //GetComponent<RawImage>().color = new Color32(9, 224, 173, 120);
        //Name = "Smart Mobile";
        //Regions.Add(this);
    }
}
